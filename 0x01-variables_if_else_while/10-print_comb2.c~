#include<stdio.h>
/**
 *main: entry point
 *description:...
 *return: 0
 */

int main(void)
{
  int num;
  for (num = 00; num <= 99; num++)
    putchar(num);
  if (num == 99)
    {
      break;
    }
  else 
    {
      putchar(',');
      putchar(' ');
    }

  putchar('\n');
  return (0);
}
