#include "holberton.h"

/**
 *main: entry point
 *description: adds two numbers
 *return: the results
 */

int add(int, int)
{
	int x , y, res;
	res = x + y;
	return res;
}

int main(void)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);
	return (0);
}
