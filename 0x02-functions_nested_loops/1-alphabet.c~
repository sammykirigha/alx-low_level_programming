#include "holberton.h"

/**
 *main: entry point
 *Description: print lowercase alphabets
 *return: 0
 */

void print_alphabet(void)
{
	char i;
	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
}

int main(void)
{
	print_alphabet();
	return (0);
}
