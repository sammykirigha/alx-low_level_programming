#include "holberton.h"

/**
 *main:entry point
 *description: prints a square
 *return: the values
 */

void print_square(int size)
{
	int i, j, k;
	if (n <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (i = 0; i <= n; i++)
		{
			for (j = 1; j <= n; j++)
			{
				_putchar('#');
			}
			_putchar('#');
			_putchar('\n');
		}
	}
}
